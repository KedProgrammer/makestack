

<%if @user %>
  <% if @user.errors.any? %>
      <div>
        <div class="alert alert-danger">
          El formulario contiene <%= pluralize(@user.errors.count,"error") %>
        </div>
        <div>
          <% @user.errors.full_messages.each do |f| %>
            <ul>
              <li><%= f %></li>
            </ul>
          <% end %>
        </div>
      </div>

  <% end %>
<% end %>
<% if @question%>
    <% if @question.errors.any? %>
        <div>
          <div class="alert alert-danger">
            El formulario contiene <%= pluralize(@question.errors.count,"error") %>
          </div>
          <div>
            <% @question.errors.full_messages.each do |f| %>
                <ul>
                  <li><%= f %></li>
                </ul>
            <% end %>
          </div>
        </div>
    <% end %>
<% end %>    
<% if @comment %>
    <% if @comment.errors.any? %>
        <div>
          <div class="alert alert-danger">
            El formulario contiene <%= pluralize(@comment.errors.count,"error") %>
          </div>
          <div>
            <% @comment.errors.full_messages.each do |f| %>
                <ul>
                  <li><%= f %></li>
                </ul>
            <% end %>
          </div>
        </div>
    <% end %>

<% end %>
<% if @answer %>
    <% if @answer.errors.any? %>
        <div>
          <div class="alert alert-danger">
            El formulario contiene <%= pluralize(@answer.errors.count,"error") %>
          </div>
          <div>
            <% @answer.errors.full_messages.each do |f| %>
                <ul>
                  <li><%= f %></li>
                </ul>
            <% end %>
          </div>
        </div>
    <% end %>

<% end %>

