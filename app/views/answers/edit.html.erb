<%= form_for( @answer) do |f| %>
    <%= render 'shared/error_messages' %>
    <%= f.label "Editar Respuesta" %>
    <br>
    <%= f.text_area :content, class:'form-control' %>

    <br>
    <%= f.submit "editar",class:'btn btn-primary' %>

<% end %>